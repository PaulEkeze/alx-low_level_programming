#include "main.h"

/**
 * print_numbers - checks for checks for a digit (0 through 9).
 **/
void print_numbers(void)
{
	int  _putchar(char);

	int number = 0;

	while (number <= 9)
	{
		_putchar(number);
		number++;
	}
	_putchar('\n');
}
