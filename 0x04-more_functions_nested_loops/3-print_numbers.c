#include "main.h"

/**
 * print_numbers - this function prints the numbers, from 0 to 9, followed by new line;
 * Return: Always 0
 */
void print_numbers(void)
{
	int  _putchar(char);
	
	char c;

	for (c = '0'; c <= '9'; c++)

		_putchar(c);

	_putchar('\n');
}
